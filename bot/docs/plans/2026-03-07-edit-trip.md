# Edit Trip Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Allow users to edit cities within existing trips — rename, change dates, remove, or add new cities.

**Architecture:** A second `ConversationHandler` with callback-query-driven entry point (`edit:{trip_id}`). States manage city selection, action selection, name input, and date picking via the existing calendar widget. Trip ID and city index stored in `context.user_data`.

**Tech Stack:** python-telegram-bot v21.6, existing storage.py JSON persistence.

---

### Task 1: Add `update_trip` to storage.py

**Files:**
- Modify: `storage.py:51` (after `delete_trip`)

**Step 1: Add `update_trip` function**

Add after `delete_trip`:

```python
def update_trip(user_id: int, trip_id: str, updates: dict) -> dict | None:
    trips = load_trips(user_id)
    for t in trips:
        if t["id"] == trip_id:
            t.update(updates)
            save_trips(user_id, trips)
            return t
    return None
```

**Step 2: Verify no syntax errors**

Run: `python -c "import storage"`
Expected: no output (clean import)

**Step 3: Commit**

```bash
git add storage.py
git commit -m "feat: add update_trip to storage"
```

---

### Task 2: Add edit button to trip display in `/trips`

**Files:**
- Modify: `main.py:260-266` (upcoming trips keyboard)
- Modify: `main.py:272-278` (archive trips keyboard)

**Step 1: Add edit button next to delete button**

Change the inline keyboard for each trip from:
```python
kb = InlineKeyboardMarkup([[
    InlineKeyboardButton("🗑 Удалить", callback_data=f"del:{tr['id']}"),
]])
```

To:
```python
kb = InlineKeyboardMarkup([[
    InlineKeyboardButton("✏️ Редактировать", callback_data=f"edit:{tr['id']}"),
    InlineKeyboardButton("🗑 Удалить", callback_data=f"del:{tr['id']}"),
]])
```

Do this in BOTH places (upcoming and archive sections).

**Step 2: Verify no syntax errors**

Run: `python -c "import bot"`
Expected: no output (clean import)

**Step 3: Commit**

```bash
git add main.py
git commit -m "feat: add edit button to trip display"
```

---

### Task 3: Define edit conversation states and data keys

**Files:**
- Modify: `main.py:70` (states line)

**Step 1: Extend states**

Change:
```python
NAME, TYPE, CITY_PICK, CITY_NAME, CITY_FROM, CITY_TO, MORE_CITIES = range(7)
```

To:
```python
(NAME, TYPE, CITY_PICK, CITY_NAME, CITY_FROM, CITY_TO, MORE_CITIES,
 EDIT_CITIES, EDIT_ACTION, EDIT_CITY_NAME, EDIT_CITY_FROM, EDIT_CITY_TO,
 EDIT_ADD_PICK, EDIT_ADD_NAME, EDIT_ADD_FROM, EDIT_ADD_TO) = range(16)
```

**Step 2: Verify no syntax errors**

Run: `python -c "import bot"`
Expected: no output

**Step 3: Commit**

```bash
git add main.py
git commit -m "feat: add edit conversation states"
```

---

### Task 4: Implement edit handlers — city list and action menu

**Files:**
- Modify: `main.py` (add new section before `# -- Delete Trip --` comment at line 556)

**Step 1: Add edit city list handler**

Add before `# -- Delete Trip --`:

```python
# -- Edit Trip ------------------------------------------------------------------

async def edit_start(update: Update, context) -> int:
    """Show list of cities in trip for editing."""
    query = update.callback_query
    await query.answer()
    trip_id = query.data.split(":")[1]
    user_id = update.effective_user.id
    trips = storage.load_trips(user_id)
    trip = next((t for t in trips if t["id"] == trip_id), None)
    if not trip:
        await query.edit_message_text("Поездка не найдена.")
        return ConversationHandler.END

    context.user_data["edit_trip_id"] = trip_id
    await _send_edit_cities(query, context, trip)
    return EDIT_CITIES


async def _send_edit_cities(query, context, trip):
    """Show city list with add button."""
    rows = []
    for i, c in enumerate(trip.get("cities", [])):
        label = f"{c['name']}  {fmt_date(c['dateFrom'])} — {fmt_date(c['dateTo'])}"
        rows.append([InlineKeyboardButton(label, callback_data=f"ecity:{i}")])
    rows.append([InlineKeyboardButton("➕ Добавить город", callback_data="ecity:add")])
    rows.append([InlineKeyboardButton("✅ Готово", callback_data="ecity:done")])

    emoji = EMOJI.get(trip["type"], "")
    name = _html(trip["name"])
    await query.edit_message_text(
        f"Редактировать: {emoji} <b>{name}</b>",
        parse_mode="HTML",
        reply_markup=InlineKeyboardMarkup(rows),
    )
```

**Step 2: Add edit action menu handler**

```python
async def edit_city_pick(update: Update, context) -> int:
    """Handle city selection or add/done."""
    query = update.callback_query
    await query.answer()
    choice = query.data.split(":")[1]

    if choice == "done":
        user_id = update.effective_user.id
        trip_id = context.user_data["edit_trip_id"]
        trips = storage.load_trips(user_id)
        trip = next((t for t in trips if t["id"] == trip_id), None)
        if trip:
            await query.edit_message_text(
                f"✅ Сохранено!\n\n{fmt_trip(trip)}",
                parse_mode="HTML",
            )
        else:
            await query.edit_message_text("Поездка не найдена.")
        context.user_data.pop("edit_trip_id", None)
        context.user_data.pop("edit_city_idx", None)
        return ConversationHandler.END

    if choice == "add":
        await query.edit_message_text("🏙 Выберите город:")
        rows = []
        for i in range(0, len(POPULAR_CITIES), 3):
            row = [
                InlineKeyboardButton(city, callback_data=f"eacity:{city}")
                for city in POPULAR_CITIES[i:i + 3]
            ]
            rows.append(row)
        rows.append([InlineKeyboardButton("✍️ Ввести вручную", callback_data="eacity:__custom__")])
        await query.message.reply_text(
            "🏙 Выберите город или введите вручную:",
            reply_markup=InlineKeyboardMarkup(rows),
        )
        return EDIT_ADD_PICK

    # Numeric city index
    idx = int(choice)
    context.user_data["edit_city_idx"] = idx

    user_id = update.effective_user.id
    trip_id = context.user_data["edit_trip_id"]
    trips = storage.load_trips(user_id)
    trip = next((t for t in trips if t["id"] == trip_id), None)
    if not trip or idx >= len(trip["cities"]):
        await query.edit_message_text("Город не найден.")
        return ConversationHandler.END

    city = trip["cities"][idx]
    label = f"{_html(city['name'])}  {fmt_date(city['dateFrom'])} — {fmt_date(city['dateTo'])}"
    kb = InlineKeyboardMarkup([
        [
            InlineKeyboardButton("✏️ Название", callback_data="eact:name"),
            InlineKeyboardButton("📅 Даты", callback_data="eact:dates"),
        ],
        [InlineKeyboardButton("🗑 Удалить город", callback_data="eact:delete")],
    ])
    await query.edit_message_text(label, parse_mode="HTML", reply_markup=kb)
    return EDIT_ACTION
```

**Step 3: Verify no syntax errors**

Run: `python -c "import bot"`
Expected: no output

**Step 4: Commit**

```bash
git add main.py
git commit -m "feat: add edit city list and action menu handlers"
```

---

### Task 5: Implement edit actions — rename, delete, dates

**Files:**
- Modify: `main.py` (add after Task 4 handlers)

**Step 1: Add action dispatcher**

```python
async def edit_action(update: Update, context) -> int:
    """Handle action choice for a city."""
    query = update.callback_query
    await query.answer()
    action = query.data.split(":")[1]
    user_id = update.effective_user.id
    trip_id = context.user_data["edit_trip_id"]
    idx = context.user_data["edit_city_idx"]

    if action == "name":
        await query.edit_message_text("✏️ Введите новое название города:")
        return EDIT_CITY_NAME

    if action == "dates":
        context.user_data["edit_date_step"] = "from"
        today = date.today()
        kb = build_month_keyboard(today.year, today.month, "efrom")
        await query.edit_message_text("📅 Новая дата заезда:")
        await query.message.reply_text("📅 Дата заезда:", reply_markup=kb)
        return EDIT_CITY_FROM

    if action == "delete":
        trips = storage.load_trips(user_id)
        trip = next((t for t in trips if t["id"] == trip_id), None)
        if not trip:
            await query.edit_message_text("Поездка не найдена.")
            return ConversationHandler.END

        if len(trip["cities"]) <= 1:
            storage.delete_trip(user_id, trip_id)
            await query.edit_message_text("🗑 Единственный город удалён — поездка удалена.")
            context.user_data.pop("edit_trip_id", None)
            context.user_data.pop("edit_city_idx", None)
            return ConversationHandler.END

        trip["cities"].pop(idx)
        storage.update_trip(user_id, trip_id, {"cities": trip["cities"]})
        trips = storage.load_trips(user_id)
        trip = next((t for t in trips if t["id"] == trip_id), None)
        await _send_edit_cities(query, context, trip)
        return EDIT_CITIES

    return EDIT_ACTION
```

**Step 2: Add rename handler**

```python
async def edit_city_name(update: Update, context) -> int:
    """Handle new city name text input."""
    new_name = update.message.text.strip()
    user_id = update.effective_user.id
    trip_id = context.user_data["edit_trip_id"]
    idx = context.user_data["edit_city_idx"]

    trips = storage.load_trips(user_id)
    trip = next((t for t in trips if t["id"] == trip_id), None)
    if not trip or idx >= len(trip["cities"]):
        await update.message.reply_text("Поездка не найдена.", reply_markup=main_keyboard())
        return ConversationHandler.END

    trip["cities"][idx]["name"] = new_name
    storage.update_trip(user_id, trip_id, {"cities": trip["cities"]})

    trips = storage.load_trips(user_id)
    trip = next((t for t in trips if t["id"] == trip_id), None)

    emoji_icon = EMOJI.get(trip["type"], "")
    name = _html(trip["name"])
    rows = []
    for i, c in enumerate(trip.get("cities", [])):
        label = f"{c['name']}  {fmt_date(c['dateFrom'])} — {fmt_date(c['dateTo'])}"
        rows.append([InlineKeyboardButton(label, callback_data=f"ecity:{i}")])
    rows.append([InlineKeyboardButton("➕ Добавить город", callback_data="ecity:add")])
    rows.append([InlineKeyboardButton("✅ Готово", callback_data="ecity:done")])

    await update.message.reply_text(
        f"Редактировать: {emoji_icon} <b>{name}</b>",
        parse_mode="HTML",
        reply_markup=InlineKeyboardMarkup(rows),
    )
    return EDIT_CITIES
```

**Step 3: Verify no syntax errors**

Run: `python -c "import bot"`
Expected: no output

**Step 4: Commit**

```bash
git add main.py
git commit -m "feat: add edit action dispatcher, rename, and delete handlers"
```

---

### Task 6: Implement edit dates (calendar reuse with efrom/eto prefixes)

**Files:**
- Modify: `main.py` (add after Task 5 handlers)

**Step 1: Add edit dateFrom calendar handler**

```python
async def edit_cal_from(update: Update, context) -> int:
    """Handle calendar for editing dateFrom."""
    query = update.callback_query
    await query.answer()
    parts = query.data.split(":")

    action = parts[1]
    if action == "noop":
        return EDIT_CITY_FROM

    if action in ("prev", "next"):
        year, month = int(parts[2]), int(parts[3])
        year, month = shift_month(year, month, -1 if action == "prev" else 1)
        kb = build_month_keyboard(year, month, "efrom")
        await query.edit_message_reply_markup(reply_markup=kb)
        return EDIT_CITY_FROM

    if action == "day":
        year, month, day = int(parts[2]), int(parts[3]), int(parts[4])
        ds = f"{year}-{month:02d}-{day:02d}"
        context.user_data["edit_new_from"] = ds
        d = date(year, month, day)
        await query.edit_message_text(f"📅 Заезд: {d.strftime('%d.%m.%Y')}")

        today = date.today()
        kb = build_month_keyboard(today.year, today.month, "eto")
        await query.message.reply_text("📅 Новая дата выезда:", reply_markup=kb)
        return EDIT_CITY_TO

    return EDIT_CITY_FROM


async def edit_city_from_text(update: Update, context) -> int:
    """Handle manual text date input for edit dateFrom."""
    ds = parse_date(update.message.text)
    if not ds:
        await update.message.reply_text("❌ Неверный формат даты. Попробуйте ДД.ММ.ГГГГ:")
        return EDIT_CITY_FROM
    context.user_data["edit_new_from"] = ds

    today = date.today()
    kb = build_month_keyboard(today.year, today.month, "eto")
    await update.message.reply_text("📅 Новая дата выезда:", reply_markup=kb)
    return EDIT_CITY_TO
```

**Step 2: Add edit dateTo calendar handler**

```python
async def edit_cal_to(update: Update, context) -> int:
    """Handle calendar for editing dateTo."""
    query = update.callback_query
    await query.answer()
    parts = query.data.split(":")

    action = parts[1]
    if action == "noop":
        return EDIT_CITY_TO

    if action in ("prev", "next"):
        year, month = int(parts[2]), int(parts[3])
        year, month = shift_month(year, month, -1 if action == "prev" else 1)
        kb = build_month_keyboard(year, month, "eto")
        await query.edit_message_reply_markup(reply_markup=kb)
        return EDIT_CITY_TO

    if action == "day":
        year, month, day = int(parts[2]), int(parts[3]), int(parts[4])
        ds = f"{year}-{month:02d}-{day:02d}"
        user_id = update.effective_user.id
        trip_id = context.user_data["edit_trip_id"]
        idx = context.user_data["edit_city_idx"]

        trips = storage.load_trips(user_id)
        trip = next((t for t in trips if t["id"] == trip_id), None)
        if not trip or idx >= len(trip["cities"]):
            await query.edit_message_text("Поездка не найдена.")
            return ConversationHandler.END

        trip["cities"][idx]["dateFrom"] = context.user_data["edit_new_from"]
        trip["cities"][idx]["dateTo"] = ds
        storage.update_trip(user_id, trip_id, {"cities": trip["cities"]})

        d = date(year, month, day)
        await query.edit_message_text(f"📅 Выезд: {d.strftime('%d.%m.%Y')}")

        trips = storage.load_trips(user_id)
        trip = next((t for t in trips if t["id"] == trip_id), None)
        await _send_edit_cities_msg(query.message, context, trip)
        return EDIT_CITIES

    return EDIT_CITY_TO


async def edit_city_to_text(update: Update, context) -> int:
    """Handle manual text date input for edit dateTo."""
    ds = parse_date(update.message.text)
    if not ds:
        await update.message.reply_text("❌ Неверный формат даты. Попробуйте ДД.ММ.ГГГГ:")
        return EDIT_CITY_TO

    user_id = update.effective_user.id
    trip_id = context.user_data["edit_trip_id"]
    idx = context.user_data["edit_city_idx"]

    trips = storage.load_trips(user_id)
    trip = next((t for t in trips if t["id"] == trip_id), None)
    if not trip or idx >= len(trip["cities"]):
        await update.message.reply_text("Поездка не найдена.", reply_markup=main_keyboard())
        return ConversationHandler.END

    trip["cities"][idx]["dateFrom"] = context.user_data["edit_new_from"]
    trip["cities"][idx]["dateTo"] = ds
    storage.update_trip(user_id, trip_id, {"cities": trip["cities"]})

    trips = storage.load_trips(user_id)
    trip = next((t for t in trips if t["id"] == trip_id), None)
    await _send_edit_cities_msg(update.message, context, trip)
    return EDIT_CITIES


async def _send_edit_cities_msg(message, context, trip):
    """Show city list (via message.reply_text, not query.edit)."""
    rows = []
    for i, c in enumerate(trip.get("cities", [])):
        label = f"{c['name']}  {fmt_date(c['dateFrom'])} — {fmt_date(c['dateTo'])}"
        rows.append([InlineKeyboardButton(label, callback_data=f"ecity:{i}")])
    rows.append([InlineKeyboardButton("➕ Добавить город", callback_data="ecity:add")])
    rows.append([InlineKeyboardButton("✅ Готово", callback_data="ecity:done")])

    emoji_icon = EMOJI.get(trip["type"], "")
    name = _html(trip["name"])
    await message.reply_text(
        f"Редактировать: {emoji_icon} <b>{name}</b>",
        parse_mode="HTML",
        reply_markup=InlineKeyboardMarkup(rows),
    )
```

**Step 3: Verify no syntax errors**

Run: `python -c "import bot"`
Expected: no output

**Step 4: Commit**

```bash
git add main.py
git commit -m "feat: add edit date handlers with calendar reuse"
```

---

### Task 7: Implement add city within edit flow

**Files:**
- Modify: `main.py` (add after Task 6 handlers)

**Step 1: Add city picker handler for edit-add**

```python
async def edit_add_pick(update: Update, context) -> int:
    """Handle city selection when adding a city during edit."""
    query = update.callback_query
    await query.answer()
    city_name = query.data.split(":", 1)[1]

    if city_name == "__custom__":
        await query.edit_message_text("🏙 Введите название города:")
        return EDIT_ADD_NAME

    context.user_data["edit_new_city"] = {"name": city_name}
    await query.edit_message_text(f"🏙 Город: {city_name}")
    today = date.today()
    kb = build_month_keyboard(today.year, today.month, "eafrom")
    await query.message.reply_text("📅 Дата заезда:", reply_markup=kb)
    return EDIT_ADD_FROM


async def edit_add_name(update: Update, context) -> int:
    """Handle custom city name input during edit-add."""
    context.user_data["edit_new_city"] = {"name": update.message.text.strip()}
    today = date.today()
    kb = build_month_keyboard(today.year, today.month, "eafrom")
    await update.message.reply_text("📅 Дата заезда:", reply_markup=kb)
    return EDIT_ADD_FROM
```

**Step 2: Add dateFrom/dateTo handlers for edit-add**

```python
async def edit_add_cal_from(update: Update, context) -> int:
    """Handle calendar for add-city dateFrom."""
    query = update.callback_query
    await query.answer()
    parts = query.data.split(":")

    action = parts[1]
    if action == "noop":
        return EDIT_ADD_FROM

    if action in ("prev", "next"):
        year, month = int(parts[2]), int(parts[3])
        year, month = shift_month(year, month, -1 if action == "prev" else 1)
        kb = build_month_keyboard(year, month, "eafrom")
        await query.edit_message_reply_markup(reply_markup=kb)
        return EDIT_ADD_FROM

    if action == "day":
        year, month, day = int(parts[2]), int(parts[3]), int(parts[4])
        ds = f"{year}-{month:02d}-{day:02d}"
        context.user_data["edit_new_city"]["dateFrom"] = ds
        d = date(year, month, day)
        await query.edit_message_text(f"📅 Заезд: {d.strftime('%d.%m.%Y')}")
        today = date.today()
        kb = build_month_keyboard(today.year, today.month, "eato")
        await query.message.reply_text("📅 Дата выезда:", reply_markup=kb)
        return EDIT_ADD_TO

    return EDIT_ADD_FROM


async def edit_add_from_text(update: Update, context) -> int:
    """Handle manual text date for add-city dateFrom."""
    ds = parse_date(update.message.text)
    if not ds:
        await update.message.reply_text("❌ Неверный формат даты. Попробуйте ДД.ММ.ГГГГ:")
        return EDIT_ADD_FROM
    context.user_data["edit_new_city"]["dateFrom"] = ds
    today = date.today()
    kb = build_month_keyboard(today.year, today.month, "eato")
    await update.message.reply_text("📅 Дата выезда:", reply_markup=kb)
    return EDIT_ADD_TO


async def edit_add_cal_to(update: Update, context) -> int:
    """Handle calendar for add-city dateTo."""
    query = update.callback_query
    await query.answer()
    parts = query.data.split(":")

    action = parts[1]
    if action == "noop":
        return EDIT_ADD_TO

    if action in ("prev", "next"):
        year, month = int(parts[2]), int(parts[3])
        year, month = shift_month(year, month, -1 if action == "prev" else 1)
        kb = build_month_keyboard(year, month, "eato")
        await query.edit_message_reply_markup(reply_markup=kb)
        return EDIT_ADD_TO

    if action == "day":
        year, month, day = int(parts[2]), int(parts[3]), int(parts[4])
        ds = f"{year}-{month:02d}-{day:02d}"
        context.user_data["edit_new_city"]["dateTo"] = ds

        user_id = update.effective_user.id
        trip_id = context.user_data["edit_trip_id"]
        trips = storage.load_trips(user_id)
        trip = next((t for t in trips if t["id"] == trip_id), None)
        if not trip:
            await query.edit_message_text("Поездка не найдена.")
            return ConversationHandler.END

        trip["cities"].append(context.user_data["edit_new_city"])
        storage.update_trip(user_id, trip_id, {"cities": trip["cities"]})

        d = date(year, month, day)
        await query.edit_message_text(f"📅 Выезд: {d.strftime('%d.%m.%Y')}")

        trips = storage.load_trips(user_id)
        trip = next((t for t in trips if t["id"] == trip_id), None)
        context.user_data.pop("edit_new_city", None)
        await _send_edit_cities_msg(query.message, context, trip)
        return EDIT_CITIES

    return EDIT_ADD_TO


async def edit_add_to_text(update: Update, context) -> int:
    """Handle manual text date for add-city dateTo."""
    ds = parse_date(update.message.text)
    if not ds:
        await update.message.reply_text("❌ Неверный формат даты. Попробуйте ДД.ММ.ГГГГ:")
        return EDIT_ADD_TO

    user_id = update.effective_user.id
    trip_id = context.user_data["edit_trip_id"]
    trips = storage.load_trips(user_id)
    trip = next((t for t in trips if t["id"] == trip_id), None)
    if not trip:
        await update.message.reply_text("Поездка не найдена.", reply_markup=main_keyboard())
        return ConversationHandler.END

    context.user_data["edit_new_city"]["dateTo"] = ds
    trip["cities"].append(context.user_data["edit_new_city"])
    storage.update_trip(user_id, trip_id, {"cities": trip["cities"]})

    trips = storage.load_trips(user_id)
    trip = next((t for t in trips if t["id"] == trip_id), None)
    context.user_data.pop("edit_new_city", None)
    await _send_edit_cities_msg(update.message, context, trip)
    return EDIT_CITIES
```

**Step 3: Add edit cancel handler**

```python
async def edit_cancel(update: Update, context) -> int:
    """Cancel edit conversation."""
    context.user_data.pop("edit_trip_id", None)
    context.user_data.pop("edit_city_idx", None)
    context.user_data.pop("edit_new_from", None)
    context.user_data.pop("edit_new_city", None)
    await update.message.reply_text("❌ Редактирование отменено.", reply_markup=main_keyboard())
    return ConversationHandler.END
```

**Step 4: Verify no syntax errors**

Run: `python -c "import bot"`
Expected: no output

**Step 5: Commit**

```bash
git add main.py
git commit -m "feat: add city add/remove handlers for edit flow"
```

---

### Task 8: Register edit ConversationHandler in main()

**Files:**
- Modify: `main.py` main() function (after the creation conv_handler registration)

**Step 1: Add edit conversation handler**

Add after `app.add_handler(conv_handler)` (line 651):

```python
    # Conversation for editing trips
    menu_btn_filter = filters.Text([BTN_CANCEL, BTN_TRIPS, BTN_DELETE, BTN_HELP, BTN_NEW])
    edit_conv = ConversationHandler(
        entry_points=[
            CallbackQueryHandler(edit_start, pattern=r"^edit:"),
        ],
        states={
            EDIT_CITIES: [
                CallbackQueryHandler(edit_city_pick, pattern=r"^ecity:"),
            ],
            EDIT_ACTION: [
                CallbackQueryHandler(edit_action, pattern=r"^eact:"),
            ],
            EDIT_CITY_NAME: [
                MessageHandler(filters.TEXT & ~filters.COMMAND & ~menu_btn_filter, edit_city_name),
            ],
            EDIT_CITY_FROM: [
                CallbackQueryHandler(edit_cal_from, pattern=r"^efrom:"),
                MessageHandler(filters.TEXT & ~filters.COMMAND & ~menu_btn_filter, edit_city_from_text),
            ],
            EDIT_CITY_TO: [
                CallbackQueryHandler(edit_cal_to, pattern=r"^eto:"),
                MessageHandler(filters.TEXT & ~filters.COMMAND & ~menu_btn_filter, edit_city_to_text),
            ],
            EDIT_ADD_PICK: [
                CallbackQueryHandler(edit_add_pick, pattern=r"^eacity:"),
            ],
            EDIT_ADD_NAME: [
                MessageHandler(filters.TEXT & ~filters.COMMAND & ~menu_btn_filter, edit_add_name),
            ],
            EDIT_ADD_FROM: [
                CallbackQueryHandler(edit_add_cal_from, pattern=r"^eafrom:"),
                MessageHandler(filters.TEXT & ~filters.COMMAND & ~menu_btn_filter, edit_add_from_text),
            ],
            EDIT_ADD_TO: [
                CallbackQueryHandler(edit_add_cal_to, pattern=r"^eato:"),
                MessageHandler(filters.TEXT & ~filters.COMMAND & ~menu_btn_filter, edit_add_to_text),
            ],
        },
        fallbacks=[
            CommandHandler("cancel", edit_cancel),
            MessageHandler(filters.Text([BTN_CANCEL]), edit_cancel),
            CommandHandler("start", fallback_start),
            CommandHandler("help", fallback_help),
            CommandHandler("trips", fallback_trips),
            CommandHandler("delete", fallback_delete),
            MessageHandler(filters.Text([BTN_TRIPS]), fallback_trips),
            MessageHandler(filters.Text([BTN_DELETE]), fallback_delete),
            MessageHandler(filters.Text([BTN_HELP]), fallback_help),
            MessageHandler(filters.Text([BTN_NEW]), fallback_start),
        ],
        per_message=False,
    )
    app.add_handler(edit_conv)
```

**Step 2: Remove the standalone `edit:` callback handler if any exists** (there shouldn't be one, but verify).

**Step 3: Verify no syntax errors**

Run: `python -c "import bot"`
Expected: no output

**Step 4: Commit**

```bash
git add main.py
git commit -m "feat: register edit conversation handler"
```

---

### Task 9: Final verification

**Step 1: Full syntax check**

Run: `python -c "import bot; import storage; print('OK')"`
Expected: `OK`

**Step 2: Commit all remaining changes**

```bash
git add main.py storage.py
git commit -m "feat: complete edit trip functionality"
```
